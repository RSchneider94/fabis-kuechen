<% if signed_in? %>
  <h4>Bewertung hinzufügen</h4>
  <%= form_for([@product, @product.comments.build]) do |f| %>
    <p>
      <%= f.label :body, "Comments" %><br>
      <%= f.text_area :body %>
    </p>
    <div class="rating"></div>
    <p>
      <%= f.submit "Submit" %>
    </p>
  <% end %>
<% else %>
  <h4>Sie müssen einloggen, um das Produkt zu bewerten.</h4>
<% end %>